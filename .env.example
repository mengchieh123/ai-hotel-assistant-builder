# Server Configuration
NODE_ENV=development
PORT=3000

# Speckit Configuration
SPECKIT_AUTO_WATCH=false

# CORS Configuration (set to true in production if needed)
ENABLE_CORS=false

# Railway will automatically set these in production:
# NODE_ENV=production
# PORT=<assigned by Railway>
