#!/bin/bash

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "📋 查看 Railway 部署日誌"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

echo "選擇日誌查看方式:"
echo "1. 最近日誌 (最後 20 行)"
echo "2. 持續查看日誌 (實時)"
echo "3. 特定部署的日誌"

read -p "請選擇 (1/2/3): " choice

case $choice in
    1)
        echo "📊 顯示最近日誌..."
        railway logs --tail 20
        ;;
    2)
        echo "🔍 開始實時日誌監控..."
        railway logs --tail 0
        ;;
    3)
        echo "📦 可用的部署:"
        railway deployments
        echo ""
        read -p "請輸入部署 ID: " deploy_id
        railway logs $deploy_id
        ;;
    *)
        echo "❌ 無效選擇，顯示最近日誌"
        railway logs --tail 20
        ;;
esac
